actor {

    // Task #1 : Define a mutable variable of type Nat called counter and initialize it to 0
    // Task #2 : Define an immutable variable of type Text called myName and set it to your name
    // Task #3 : Define a query function called myName that returns your name
    // Task #4 : Define a query function called getCounter that returns the value of the counter variable
    // Task #5 : Define an update function called incrementCounter that increments the value of the counter variable by 1.
    // Task #6 : Define an update function called resetCounter that sets the value of the counter variable to 0.
    // Task #7 : Define an update function called setCounter that sets the value of the counter variable to the value of the argument passed to the function.
}